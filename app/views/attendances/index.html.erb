<h1>Asistencia</h1>

<table>
  <tr>
    <th>Rut</th>
    <th>Month</th>
    <th>Action</th>
  </tr>
 
  <% @attendances.each do |attendance| %>
    <tr>
      <td><%= attendance.rut %></td>
      <td><%= attendance.month %></td>
      <td><%= attendance.action %></td>
    </tr>
  <% end %>
</table>

<%= form_tag import_attendances_path, multipart: true do %>
  <%= file_field_tag :file %>
  <%= submit_tag "Import attendance" %>
<% end %>
